extends layout

block content
  h1= title
  p Welcome to #{title}

  if survey_list.length
    ul
      each survey in survey_list
        li
          a(href=survey.url) #{survey.title}

  else
    p There are no surveys.

